# Zero Tilt Poker — contexto Hermes (monitoramento)

> Este arquivo é lido pelo Hermes Agent quando o `workdir` aponta para este repo.
> Postura do piloto: **somente observar e reportar. Nunca alterar código, banco, infra ou docs.**

## Fontes canônicas (ler antes de qualquer verificação)

- `Documentacao/STATUS_OPERACIONAL.json` — ciclo vigente, catálogo cash/MTT, wallets, limites (prevalece sobre tudo).
- `.agents/AGENTS.md` — memória operacional do projeto.
- `Documentacao/DEMO_AMIGOS.md` — jornada esperada do jogador.

## O que monitorar

- `GET /api/health` — stack 4/4 healthy.
- `GET /api/lobby/tables` — catálogo S21 (NL 9-max · SD Texas 8-max · SD Omaha 5-max · Pineapple 6-max, PM + Real).
- `GET /api/presence/online` — contador coerente.
- Torneios agendados 21:30 America/Sao_Paulo com auto-start 5+.

## Disciplina de reporte (anti-estresse)

- Saudável = `[SILENT]`: nada para dizer, não diga.
- Só reporte proativamente o crítico: API fora do ar, stack não-healthy, catálogo divergente do S21, MTT 21:30 em risco.
- O resto vai para o digest diário ou fica em arquivo local. A mesma falha não repete o alerta — use `incidents ack`.

## Proibições do piloto

Sem deploys, migrations, commits, escrita em banco, PIX/payout ou qualquer mutação. Dúvida sobre agir → pergunte ao usuário, nunca aja por conta própria.
